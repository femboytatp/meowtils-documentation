# **Prefix**

This allows you to change the prefix **Meowtils** uses for all messages.

!!! Warning

    Keep in mind this will conflict with other extensions if they also try to set prefix.

---

### Set

``` java
Prefix.setPrefix(String prefix)
```

### Reset

``` java
resetPrefix()
```